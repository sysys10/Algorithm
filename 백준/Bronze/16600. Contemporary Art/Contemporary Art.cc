#include <bits/stdc++.h>
using namespace std;
void solve()
{
    double n;
    cin >> n;
    printf("%.8f", sqrt(n) * 4);
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
}