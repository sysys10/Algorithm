#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    int a, v, c, d, e, f;
    cin >> a >> v >> c >> d >> e >> f;
    cout << (a + v + c + d + e + f) * 5;
}