#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n, m;
    cin >> n >> m;
    cout << min(n/2,m/2);
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
}
