#include <bits/stdc++.h>
using namespace std;
void solve()
{
    int a, b, c;
    cin >> a >> b >> c;

    a + b <= c ? cout << 1 : cout << 0;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
}