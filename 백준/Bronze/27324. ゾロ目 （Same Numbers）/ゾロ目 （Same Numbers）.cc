#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    char str[3];
    cin >> str;
    if(str[0]==str[1]){
        cout << 1;
    }else
        cout << 0;
}