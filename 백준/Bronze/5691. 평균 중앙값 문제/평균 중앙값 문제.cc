#include <bits/stdc++.h>
using namespace std;

vector<long long> v;

void solve()
{
    
    int t;
    long long n, m, k = 0, sum = 0, cnt = 0, max = 0;
    string str, s;

    while (1)
    {
        cin >> n >> m;
        if (n == 0 && m == 0)
            break;
        cout << n - (m - n) << '\n';
    }

}

int main()
{
    solve();
}